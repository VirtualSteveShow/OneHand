# OneHand — To-Do List

| Status | Item | Notes |
|--------|------|-------|
| ✅ Done | v1 Project scaffold | Hub shell (`index.html`/`style.css`/`hub.js`), PWA manifest + service worker, aiohttp local dev server (port 8084), GitHub Pages Actions workflow, placeholder icons. Empty `GAMES` array — hub shows "No games yet" until the first game is added. |
| ⬜ To Do | Real icon art | `generate_icons.py` currently produces a placeholder (dark rounded square + accent dot). Replace with real branding once the app has an identity. |
| ✅ Done | First game — Flap | Built 2026-07-05. Flappy-Bird-style tap-to-flap dodger at `public/games/flap/`. Single input vocabulary (tap = flap/start/retry), canvas-based, ceiling is a soft boundary (only ground + pipes are lethal) so spam-tapping near the top never causes a confusing death. Best score in localStorage. Added to `GAMES` in hub.js, shares the hub's service worker cache. Verified with Playwright using real `Touch`/`TouchEvent` dispatch (not `.click()`) — tap-to-start, flap physics, pipe collision, game-over, retry, and back-to-hub all confirmed working. |
| ✅ Done | Second game — Dash | Built 2026-07-05. Swipe-only 3-lane endless runner at `public/games/dash/` — swipe left/right changes lane, up jumps low barriers, down ducks overhead bars, only one obstacle spawns at a time so a lane switch is always a valid escape. Tap only used for start/retry menus. Best score in localStorage. Added to `GAMES` in hub.js, shares the hub's service worker cache. Verified with Playwright using real `Touch`/`TouchEvent`/`PointerEvent` dispatch — swipe classification, lane changes, jump/duck rendering, and (via a deterministic forced-collision check) the game-over → retry → start state cycle all confirmed working. |
| ✅ Done | Third game — Charge | Built 2026-07-05. Hold-to-charge, release-to-launch precision-timing game at `public/games/charge/` — press and hold to ride a triangle wave 0→1→0, release to lock in that charge, land inside the (shrinking, speeding-up) target zone to score. A tap is just a near-zero-duration hold, so no tap/hold disambiguation delay was needed. Best score in localStorage. All 3 games now cover the full one-handed gesture set: Flap=tap, Dash=swipe, Charge=hold. Verified with Playwright using real `Touch`/`Pointer` event dispatch for press/hold/release timing, plus deterministic target-window overrides to force both a hit and a miss and confirm the full start→playing→result→over→retry cycle. |
| ✅ Done | Decide shared vs. per-game service worker caching | Decided 2026-07-05: staying with one shared cache (`sw.js`'s `ASSETS` array) now that 3 games exist. Combined asset size across all games is trivially small, and since fetch is network-first with cache only as an offline fallback, there's no real benefit to per-game isolation — revisit only if a future game adds substantial offline assets (e.g. images/audio). |
| ✅ Done | Fourth game — Orbit | Built 2026-07-05. New input primitive at `public/games/orbit/`: a floating radial wheel spawns wherever you press down (no fixed on-screen target to hit), and dragging your thumb around that spawn point in a small arc sets an angle in real time — the felt sensation of "spinning" a wheel under your thumb. Player is a dot fixed on a ring; inbound debris arcs fall from outside toward the ring at a fixed angle (same fairness model as Dash's lanes, but continuous angle instead of 3 discrete ones) and must be rotated away from. Releasing freezes the angle. The wheel can spawn anywhere, but the playfield (ring + debris) stays fixed in the upper-middle of the screen so the thumb/wheel never covers the action. Best score in localStorage. All 4 games now cover the full one-handed gesture set: Flap=tap, Dash=swipe, Charge=hold, Orbit=radial drag. Verified with Playwright using real dispatched `touchmove`/`pointermove` events at 8 angles around the wheel to confirm `playerTheta` tracks the drag precisely, plus freeze-on-release and a deterministic forced-collision check for the game-over → retry cycle. |
| ✅ Done | Fifth game — Sling | Built 2026-07-05. New input primitive at `public/games/sling/`: pull-back-and-release. Press anywhere to plant an anchor at the touch point (floats like Orbit's wheel), drag away from it to pull back a rubber-band, release to launch a ball on a gravity arc — direction AND pull distance both matter, the first mechanic here to encode two continuous values in one drag instead of one. A dotted trajectory preview renders while aiming. Aiming is by pulling away from the target, never by touching it, so all input stays low on screen regardless of where the target sits (a design constraint the user flagged: the thumb can only reach the bottom of the screen while holding a baby in the other arm). Best score in localStorage. Verified with Playwright using real touch press/drag/release — confirmed real gravity-driven flight, a genuine hit via a target placed at the ball's actual mid-flight position, and a genuine miss by pulling the opposite direction to send the ball off the bottom of the screen. **Fixed 2026-07-05:** real-device testing showed some targets were unreachable even at max pull (reachable envelope math: max height was only `v²/2g` ≈ 278px with the original 850 px/s speed / 1300 px/s² gravity, far short of the ~700px+ climb needed from a bottom-anchored shot to a top-corner target). Reworked to 1400 px/s max speed / 1000 px/s² gravity, verified against the worst-case corner-to-corner case with the actual "parabola of safety" formula (`Y_up <= v²/2g - g·dx²/2v²`) plus a live Playwright test computing the exact launch angle and confirming a real hit. |
| ✅ Done | Sixth game — Tilt (gyro) | Built 2026-07-05. New input primitive at `public/games/tilt/`: tilt the phone left/right to steer a ball continuously (not discrete lanes) around single falling blocks (same one-at-a-time fairness model as Dash/Orbit). Handles iOS's `DeviceOrientationEvent.requestPermission()` prompt (required before iOS 13+ Safari will deliver motion events at all; Android needs no such prompt), with a "MOTION ACCESS NEEDED" message shown on the start screen if permission is denied. Best score in localStorage. **Revised 2026-07-05:** originally required holding a finger down to arm the gyro (mitigating the CLAUDE.md caution that a baby-holding hand also rocks unpredictably) — user found this defeated the point of a tilt control and asked for it to work with zero touch once a run starts. Removed the hold-to-arm gate entirely; tilt now reads continuously during play from a single tap-to-start. Remaining mitigations against false-triggers: single axis only (left/right), a deadzone around zero, and the zero-point is still recalibrated fresh every run from whatever angle the phone is at when that run's start-tap happens. This is a real, deliberate trade-off — the "only reads while held" protection is gone — but the user made the call twice (first to build gyro despite the flagged risk, then explicitly to drop the hold requirement). Verified with Playwright dispatching synthetic `deviceorientation` events with zero touch events present after the initial start tap — confirmed the ball steers left/right/back-to-center purely from tilt, no finger involved. **Known test gap (unchanged):** the iOS permission-prompt path itself still can't be exercised in Chromium-based automated testing — needs a real iOS device check. |
| ✅ Done | Seventh game — Flick | Built 2026-07-05. New input primitive at `public/games/flick/`: momentum toss. Swipe anywhere on screen (same "whole screen is the gesture zone" trick Dash used — the gesture doesn't need to touch the puck) but the *release velocity* itself, not just direction (Dash) or pull distance (Sling), launches a puck from a fixed tee position. Only the last ~90ms of motion before release is sampled for velocity, clamped to a max speed; too-slow releases cancel rather than firing a doomed shot (same forgiveness as Sling's MIN_PULL). Puck then slides under real friction and bounces elastically off all 4 walls until its speed drops below a threshold, then scores if it rests inside the (shrinking) target zone. Best score in localStorage. Verified with Playwright dispatching real timestamped touchmove sequences — confirmed a slow drag correctly cancels with no shot fired, a fast flick produces genuine nonzero velocity and the puck visibly moves under its own physics between two samples, and (via deterministic position/velocity overrides) both the hit → next-round and miss → game-over paths. Fixed a minor bug found during verification: `playState` was left stale at `'moving'` after a miss instead of resetting to `'idle'` (harmless since `state !== PLAYING` already blocks further updates, but corrected for consistency). |
| ✅ Done | Hub — make the game list scrollable | Fixed 2026-07-05. With 8 games, the hub's `html, body { overflow: hidden; height: 100% }` clipped the list on shorter screens — the last card (Gaze) was cut off with no way to scroll to it (user-reported via screenshot). This only affects the hub's own `style.css`; each game's own stylesheet correctly keeps `overflow: hidden` since those are full-screen canvas games that need to own all gestures. Changed to `overflow-y: auto` + `min-height: 100%` (was a fixed `height: 100%`, which prevented the page from ever growing taller than the viewport). Verified with Playwright at a viewport short enough to force real overflow (390×700) — confirmed `scrollHeight > clientHeight`, that scrolling actually moves `scrollY`, and that the Gaze card is fully reachable and visible after scrolling. |
| ✅ Done | Eighth game — Gaze (Blink mode) | Built 2026-07-05, confirmed working well on a real device 2026-07-06. First non-touch, non-gyro input primitive at `public/games/gaze/`: real-time face/eye tracking via the front camera, using Google's MediaPipe Tasks Vision `FaceLandmarker` (dynamically imported from jsdelivr's CDN — the only external dependency in the whole hub, see CLAUDE.md's Stack section). Blink mode reuses Flap's exact bird/pipe physics unmodified, with a blink (onset-detected from the `eyeBlinkLeft`/`eyeBlinkRight` blendshape scores crossing a threshold) as the trigger instead of a tap. START/RETRY still happen via a tap, but the actual flap action is blink-only. Shows a small live camera preview thumbnail so the player can confirm they're in frame; video is processed fully on-device, never sent anywhere. |
| ✅ Done | Gaze — Area mode + mode-select screen | Built 2026-07-06. Second mode in `public/games/gaze/`, per user request that all modes live in one game. Restructured gaze.js around a mode-select screen shown once the camera/model loads (tap BLINK or AREA); the camera/model session is loaded exactly once and shared — switching modes via the in-game "CHANGE MODE" hint (shown on each mode's own start screen) doesn't reload it. Area mode reuses Dash's 3-lane logic driven by coarse look-direction: MediaPipe's ARKit-style `eyeLookInLeft/OutLeft/InRight/OutRight` blendshapes combine into a -1..1 gaze signal, smoothed (EMA) and classified into left/center/right, animated the same lane-lerp way as Dash. Unlike Dash there's no jump/duck fallback, so rows only ever fill 1-2 of the 3 lanes (never 3) — a lane must always be free purely from where you're looking; scoring is once per row, same fix as Dash's. Zero touch during play (only the initial tap-to-start), matching Tilt's precedent. Verified the gaze signal → smoothing → classification → lane-lerp pipeline converges correctly, mode-select tap targets route correctly, CHANGE MODE returns without reloading the model, and a forced collision/retry cycle works. **Testing note:** Chromium's fake camera has no real face, so (same limitation as Blink's initial build) only the downstream pipeline was verified, not MediaPipe's real-world look-direction accuracy — needs a real-device check. |
| ✅ Done | Gaze — Full Detection mode (3rd and final planned mode) | Built 2026-07-06. Continuous calibrated gaze-point tracking in `public/games/gaze/`, the most technically ambitious of the three modes. A 5-point look-and-tap calibration (center + 4 corners, tap anywhere while looking at the highlighted point) captures the same look-direction blendshape signal Area uses — now read on both axes (added `eyeLookUp/DownLeft/Right` alongside the existing left/right ones) — paired with the known target screen position, then fits a small linear regression (`screenAxis = a·gazeX + b·gazeY + c`) via hand-rolled least squares (3x3 Gaussian elimination on the normal equations — no external calibration library exists for this, had to write it). Gameplay: targets need a **cumulative** (not strict) dwell of 500ms within their radius before a shrinking time budget runs out; looking away doesn't reset progress, only pauses it — a deliberate forgiveness given the accuracy uncertainty. A live crosshair cursor is always shown so the player gets direct feedback on where the system thinks they're looking, plus a dwell-progress ring around the target. Calibration is cached per session (not repeated on every mode switch) with a "RECALIBRATE" hint alongside "CHANGE MODE" on the mode's start screen. Verified the regression solver recovers a known synthetic linear relationship exactly (proving the math itself is correct, independent of any real camera signal), and — via deterministic calibration-coefficient overrides that avoid fighting the live per-frame `applyCalibration()` call (same class of fix needed for Area's testing) — confirmed dwell genuinely accumulates and scores, and a genuine miss (time budget expires with the predicted point held off-target) ends the run. All 3 planned Gaze modes are now built. |
| ✅ Done | Gaze — real-device feedback: Blink flawless, Area needs calibration | User tried all 3 modes for real 2026-07-06. **Blink**: flawless, "could 100% be used as an input" — no changes needed. **Area**: worked, but switching to the right lane was much harder to trigger than left/center. Root cause: Area had no calibration at all, just a fixed symmetric ±0.10 threshold assuming the user's natural left/right gaze range is symmetric around center — it usually isn't (camera angle, individual anatomy, etc.), so a fixed threshold makes whichever direction has a naturally smaller range feel broken. **Fixed** by adding a 3-step calibration (look left, look center, look right; hold and tap each) that computes per-user, independently-sized left/right thresholds from the user's own observed range, same session-cached + "RECALIBRATE" hint pattern as Full mode. Verified with a simulated asymmetric user (strong left range, weak +0.06 right range) that the old fixed threshold would never have registered as right, but the new calibrated threshold correctly does. **Full Detection**: user wants to hold off re-evaluating until Area feels solid. **On the "irregular pupils" / dominant-eye question:** the code averages both eyes' blendshape scores for every signal (blink and both gaze axes) rather than using one eye alone; whether pupil irregularity specifically causes uneven left/right response is genuinely unconfirmed — a fixed uncalibrated threshold produces the identical symptom regardless of eye anatomy, so that was fixed first as the simpler, cause-agnostic explanation. Single-eye-only tracking (to isolate a specific eye if one is less reliable) remains a candidate follow-up if the calibrated version still shows a persistent asymmetry, but wasn't needed yet. |
| ✅ Done | Balance tweaks — Flap, Dash, Orbit | Fixed 2026-07-05, from user real-device playtesting feedback. **Flap** previously had zero difficulty ramp (constant pipe speed/gap/interval forever) — added a modest ramp on all three (speed 190→340 px/s, gap 230→165px, interval 1500→1000ms, each with a floor/cap) so a run gets steadily harder rather than staying flat. **Dash** always spawned exactly 1 obstacle at a time, leaving 2 lanes permanently open — user found this let a lane-swipe trivially dodge everything regardless of type, making jump/duck nearly pointless after a few minutes. Redesigned to spawn "rows" occupying 1–3 lanes at once (more lanes as score climbs); rows filling only 1–2 lanes can use any obstacle type (a lane always stays open as an escape), but a row filling all 3 lanes never includes the lane-switch-only `full` type, so whichever lane the player's in always has a genuine jump/duck answer — verified both the always-solvable guarantee (0/500 forced full-type picks in 3-lane rows) and a genuine unavoidable-by-luck miss when the wrong action is taken. Also fixed scoring to count once per row, not once per obstacle, so the difficulty ramp (which reads `score`) doesn't inflate faster just from lane count. **Orbit** ("Hold and Spin") redesigned per user request from "small danger arc, mostly-safe ring" to "thread the needle": each wave is now a near-complete ring with exactly one gap (the safe opening), which narrows with score (0.55→0.28 rad) instead of the previous fixed-width small danger arc — inverts which side is hazard vs. safe. Also fixed debris/rings visibly popping into existence mid-screen — spawn radius is now computed from the actual farthest screen corner (`Math.hypot` to the corner + margin) so waves are always genuinely off-screen at spawn regardless of aspect ratio, verified spawn radius (646px) exceeds max corner distance (606px) at a 390×844 viewport. Verified threading the gap survives + scores, missing it is a genuine game-over. |
